const greet = () => alert('Hello');
setTimeout(greet, 1000);

//const greetWithName = (name) => alert(`Hello ${name}`);
//setTimeout(() => greetWithName('Marco'), 1000);

//let name = 'Steve';
//setTimeout(() => greetWithName(name), 1000);

/*
const adder = (addValue) =>
  (x) => addValue + x;

const add3 = adder(3);

add3(7);
*/
