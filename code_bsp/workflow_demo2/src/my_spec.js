"use strict";

import Board from './board.js';

describe("something", () => {
  xit("should", () => {
    expect((new Board()).x).toEqual(27);
  });
  it("should", () => {
    expect(3).toEqual(1 + 2);
  });
});
